/**
 * This file is only used for webpack-dev-server.
 */
'use strict';
// var ExtractTextPlugin = require("extract-text-webpack-plugin");
var webpack = require('webpack');
var path = require('path');
var HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
    context: path.join(__dirname, './'),
    entry: {
        js: './client/src/index.js'
    },
    output: {
        path: path.join(__dirname, './dist'),
        filename: 'bundle.js',
    },
    module: {
        loaders: [  
            {
                test: /\.(scss|css)$/,
                loader: "style!css?modules&importLoaders=1&localIdentName=[name]__[local]___[hash:base64:5]!sass"
            },
            {
                test: /\.js$/,
                exclude: /node_modules/,
                loaders: [
                    'react-hot-loader',
                    'babel-loader'
                ]
            }
        ],
    },
    resolve: {
        alias: {},
        extensions: ['','.js', 'jsx']
    },
    plugins: [
        new webpack.DefinePlugin({
            'process.env': { NODE_ENV: JSON.stringify(process.env.NODE_ENV || 'local') },
            ENVIR: JSON.stringify('local')
        }),
        new HtmlWebpackPlugin({
            template: './client/src/index.html', // Move the index.html file...
            inject: true // inject all files that are generated by webpack, e.g. bundle.js, style.css with the correct HTML tags
        }),
    ],
    devtool: 'source-map',
    devServer: {
        hot: true
    }
};
